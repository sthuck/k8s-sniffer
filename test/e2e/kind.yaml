# kind cluster config for k8s-sniffer e2e.
# Two nodes so E2E2.3 can pin workloads to distinct nodes (T-TEST.4).
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
